# 옷입히기 게임 — AI 인수인계 핸드아웃

> 이 문서는 **다른 AI 툴에게 이 프로젝트를 이해시키기 위한** 자료입니다.
> 특히 지금 목표는 **게임에 넣을 "옷 PNG" 이미지를 다른 AI 그림 툴로 제작**하는 것이므로,
> 뒤쪽 [5. 옷 PNG 제작 스펙](#5-옷-png-제작-스펙-그림-ai-툴에게-줄-지시서) 부분이 가장 중요합니다.
> (그림 AI에게는 이 문서 전체가 아니라 5번 섹션만 복사해서 줘도 됩니다.)

---

## 1. 프로젝트 한눈에 보기

- **무엇인가**: 웹 브라우저에서 하는 **캐릭터 옷입히기 게임**.
- **누가 만드나**: 비개발자/기획자가 개발 학습 + 취미로 제작. **배포하지 않고 로컬(내 컴퓨터)에서만** 실행.
- **어떻게 실행하나**: 빌드 도구 없음. `index.html`을 브라우저로 열면 바로 실행됨.
- **기술 스택**: 순수 HTML + CSS + JavaScript (프레임워크 없음).

### 게임의 5단계 흐름
1. **캐릭터 선택** — 여러 캐릭터 중 하나를 고름.
2. **옷 입히기** — 캐릭터의 **대사(힌트)만 보고** 어울릴 것 같은 옷을 골라 입힘.
3. **숨은 요구조건** — 캐릭터마다 "이런 옷을 원해요"라는 평가 기준이 있지만 **유저에게는 안 보임**. 유저는 대사로만 유추함. (이 기준은 나중에 LLM에게 넘길 프롬프트 문장으로 저장돼 있음.)
4. **런웨이** — 다 입히면 캐릭터가 무대를 걸어감 (애니메이션 + 사운드).
5. **평가** — 입은 옷을 채점해서 점수·코멘트를 보여줌. **지금은 가짜(규칙 기반) 평가**, 나중에 진짜 Claude LLM으로 교체 예정.

---

## 2. 파일 구조

```
ax-first-project/
├─ index.html      화면 뼈대 (3개 화면 + 팝업들)
├─ style.css       디자인/애니메이션
├─ game.js         게임 로직 (데이터 + 렌더링 + 사운드 + 평가) ← 핵심
├─ images/
│  ├─ Pico_muscle.png   피코 캐릭터 전신 이미지 (1114×1411)
│  └─ README.md
└─ HANDOFF.md      (이 문서)
```

3개 화면은 `index.html` 안에 다 들어있고, JS가 `hidden` 클래스를 껐다 켜서 전환합니다.
- `#screen-select` (캐릭터 고르기)
- `#screen-dressup` (옷 입히기)
- `#screen-runway` (런웨이)

---

## 3. 화면 좌표계 (매우 중요)

캐릭터와 옷은 전부 **SVG `viewBox="0 0 300 400"`** 안에 그려집니다.
즉 **가로 300 × 세로 400짜리 좌표 도화지** 위에 모든 것이 얹힙니다.

- 캐릭터 전신 이미지(피코)는 이 도화지의 `x=2, y=8, width=296, height=384` 위치에 깔립니다.
- 옷은 그 **위에 겹쳐서** 그려집니다 (레이어 순서: 신발 → 상의 → 모자).

```
(0,0) ┌───────────────┐  ← viewBox 좌상단
      │   피코 이미지   │
      │   위에 옷을     │
      │   얹는 구조     │
      └───────────────┘ (300,400) ← viewBox 우하단
```

---

## 4. 데이터 모델 (game.js)

### 4-1. 캐릭터 (`characters` 배열)
```js
{
  id: "pico",
  name: "피코",
  bodyImage: "images/Pico_muscle.png",  // 전신 이미지 경로
  quote: "음~ 오늘은 지적이면서도 센스 있는 룩이면 좋겠어...",  // ★유저에게 보이는 유일한 힌트
  evalPrompt: "피코는 똑똑하고 개성 있는 스타일을...",          // ★유저에게 안 보임. 나중에 LLM 채점용
  likeTags: ["unique"],      // (임시 가짜평가용) 좋아하는 옷 태그
  dislikeTags: ["sporty"],   // (임시 가짜평가용) 싫어하는 옷 태그
}
```
- **`quote`(보임) vs `evalPrompt`(숨김)의 분리가 이 게임의 핵심 재미**입니다. 유저는 대사만 보고 숨은 취향을 추리합니다.

### 4-2. 옷 (`wardrobe` 배열) — **지금 여기가 교체 대상**
현재 옷은 **손으로 그린 SVG 도형**으로 되어 있어 퀄리티가 낮습니다. 예:
```js
{
  id: "hat_straw", category: "hat", name: "밀짚모자",
  tags: ["cute", "casual"],           // (가짜평가용 성격 태그)
  onBody: `<ellipse .../><path .../>`, // ★캐릭터 위에 겹쳐 그릴 SVG (← 이걸 PNG로 바꿀 예정)
  icon:   `<ellipse .../>`,            // 옷장 버튼용 작은 아이콘 SVG
}
```
- 카테고리(`category`)는 현재 5종: **`hat`(모자) / `top`(상의) / `bottom`(하의) / `dress`(원피스) / `shoes`(신발)**. 각 카테고리당 하나씩만 착용 가능.
- **상·하의 vs 원피스 규칙**: 원피스는 상체~하체를 한 벌로 덮는 옷이라 **상의·하의와 동시에 입을 수 없습니다.** 코드에서 자동 처리됨 — 원피스를 입으면 상의·하의가 벗겨지고, 상의나 하의를 입으면 원피스가 벗겨집니다.
- **겹치는 순서(뒤→앞)**: 신발 → 하의 → 상의 → 원피스 → 모자.
- **계획**: `onBody`(손그림 SVG)를 **투명 배경 PNG 이미지**로 교체. (제작 스펙은 아래 5번.)

---

## 5. 옷 PNG 제작 스펙 (그림 AI 툴에게 줄 지시서)

> **이 섹션만 그림 AI에게 복사해서 줘도 됩니다.** 함께 피코 캐릭터 이미지(`images/Pico_muscle.png`)를 참고용으로 제공하세요.

### 5-1. 캐릭터 설명 (피코)
- **피코**는 보라색 새 캐릭터로, **얼굴(안경 쓴 새 머리)은 그대로**이고 **목 아래는 우스꽝스러운 근육질 사람 몸**입니다. (포켓몬 "디그다=근육질 몸매" 밈 느낌의 코믹 콘셉트.)
- 옷은 이 **근육질 사람 몸에 입히는** 것입니다.

### 5-2. PNG 파일 규격
- **캔버스 크기**: 세로로 긴 **1200 × 1600 px** (가로:세로 = 3:4, 게임 도화지와 동일 비율). 피코 원본(1114×1411)과 맞춰도 됩니다. **모든 옷을 같은 크기·같은 정렬로** 만드는 것이 핵심.
- **배경**: 반드시 **완전 투명(alpha) 배경**. 옷 부분만 그리고 나머지는 투명.
- **정렬(가장 중요)**: 각 옷 PNG는 **피코 전신 위에 그대로 겹치면 딱 맞도록**, "피코가 그 옷을 입은 모습"에서 **피코 몸만 지우고 옷만 남긴 것**처럼 만들어야 합니다. 즉 캔버스 안에서 옷의 위치가 곧 최종 착용 위치입니다. (옷을 캔버스 정중앙에 따로 떠 있게 그리면 안 됨.)
- **한 파일 = 한 옷 한 벌**. 모자면 모자만, 상의면 상의만 그린 투명 PNG.

### 5-3. 피코 몸 랜드마크 (옷을 어디에 놓을지 기준 — 캔버스 높이 대비 %)
캔버스 맨 위 0%, 맨 아래 100% 기준입니다. (좌우는 가로 폭 대비 %)

| 부위 | 세로 위치 | 좌우 범위 | 옷 배치 가이드 |
|---|---|---|---|
| 머리 돔 꼭대기 | ~6% | 중앙 | **모자**: 여기에 얹힘 (안경 위, 머리 위) |
| 눈/안경 | ~21% | 중앙 | 모자가 이 선을 가리지 않게 |
| 어깨 선 | ~30% | 넓음 | **상의** 시작 (근육 어깨라 매우 넓음) |
| 가슴(몸통 최대폭) | ~37% | **17%~83%** | 상의 몸통이 이 폭을 감쌈 |
| 상의 밑단 | ~57% | 중앙 | 상의는 대략 여기까지 (허리 위) |
| 허리/골반 | ~68% | 중앙 | **하의** 시작 (허리에 걸침) |
| 다리 시작 | ~71% | 중앙 | 여기서 두 다리로 갈라짐 |
| 발/바닥 | ~92%~96% | **30%~69%** (중앙 49%에서 좌우로 갈라짐) | **신발**: 두 발이 벌어져 있으니 좌·우 신발을 각각 배치 |

- **모자(hat)**: 세로 4~10% 영역, 중앙.
- **상의(top)**: 세로 30~57% 영역, 좌우 17~83% 폭을 감싸도록. (근육이 옆으로 삐져나오는 건 콘셉트상 OK.)
- **하의(bottom)**: 세로 68~92% 영역. 허리(중앙)에서 시작해 **다리가 좌우로 벌어지며 발(30~69% 폭)까지** 내려감. 바지는 양 다리, 치마는 종 모양으로.
- **원피스(dress)**: 세로 30~85% 영역 (상의+하의를 한 벌로). 어깨~허벅지까지 하나로 덮음. **상·하의와 겹쳐 입지 않으므로** 몸통 전체를 자유롭게 디자인.
- **신발(shoes)**: 세로 90~97% 영역, 발이 좌우로 벌어져 있으므로 **양발 각각** 그림.

### 5-4. 아트 스타일
- 밝고 귀여운 톤 (게임 UI가 핑크/보라 파스텔 계열).
- 옷 자체는 카툰/플랫 일러스트 느낌으로 또렷하게. 그림자·외곽선 약간 있으면 좋음.
- 배경 투명 필수 (다시 강조).

### 5-5. 만들면 좋은 옷 목록 (예시)
현재 임시 도형으로 들어있는 옷들을 **고퀄 PNG로 대체 + 확장**하고 싶습니다. 카테고리별로 다양하게:
- **모자(hat)**: 밀짚모자, 왕관, 베레모, 캡모자 …
- **상의(top)**: 티셔츠, 블라우스, 정장 재킷, 후드티, 니트 …
- **하의(bottom)**: 청바지, 치마, 반바지, 슬랙스 …
- **원피스(dress)**: 핑크 원피스, 드레스, 점프수트 … (상·하의를 한 벌로 덮는 옷)
- **신발(shoes)**: 운동화, 구두, 부츠, 샌들 …

> 참고: **상의/하의는 각각 따로**, **원피스는 한 벌**로 만들어 주세요. 상의는 허리 위(세로 57%)까지만, 하의는 허리 아래만 그리면 자연스럽게 겹칩니다.

---

## 6. 만든 PNG를 게임에 넣는 방법 (개발자/코드 담당용)

옷 PNG를 `images/clothes/` 같은 폴더에 넣고, `game.js`의 `wardrobe` 항목에서
`onBody`(손그림 SVG)를 **이미지 태그로 교체**하면 됩니다. 캔버스가 도화지와 같은 정렬이므로 전체를 덮게 배치합니다:

```js
{
  id: "hat_straw", category: "hat", name: "밀짚모자", tags: ["cute","casual"],
  onBody: `<image href="images/clothes/hat_straw.png" x="0" y="0" width="300" height="400"
                  preserveAspectRatio="xMidYMid meet"/>`,
  icon: `...`  // 옷장 버튼 아이콘 (작은 PNG나 SVG)
}
```
- 렌더링·착용·런웨이 로직은 이미 `onBody` 문자열을 그대로 SVG에 삽입하는 구조라, **`onBody` 내용만 PNG `<image>`로 바꾸면 나머지는 수정 불필요**합니다.
- (아이콘 `icon`도 옷장 버튼에 쓰이므로 작은 썸네일 PNG를 별도로 주거나, 기존 SVG 아이콘을 유지해도 됩니다.)

---

## 7. 평가 로직 (나중에 진짜 AI로 교체할 지점)

- 채점은 `game.js`의 **`evaluateOutfit(character, worn)` 함수 한 곳**에서만 일어납니다.
- 지금은 인터넷/AI 없이 **규칙 기반 가짜 점수**(태그 매칭 +12 / 감점 -15 / 풀착용 +15, 기본 60점)를 냅니다.
- 나중에 이 함수 안에서 `character.evalPrompt`(숨은 기준)와 입은 옷 목록을 **Claude LLM에 보내 점수·코멘트를 받아오도록** 바꾸면 5단계가 "진짜 AI 평가"로 완성됩니다. **이 함수만 갈아끼우면 됨.**

---

## 8. 현재 상태 & 다음 할 일

**완료됨**
- 5단계 전체 뼈대 (선택→입히기→런웨이→평가) 동작.
- 게임 연출: 확인 팝업, 걷기 애니메이션, 두구두구 드럼롤, 효과음/BGM(사운드 파일 없이 Web Audio로 합성).
- 실제 캐릭터 **피코** 적용 + 옷 위치를 피코 몸 윤곽에 맞게 정밀 조정.

**지금 하려는 것 (이 문서의 목적)**
- 손그림 SVG 옷 → **그림 AI로 만든 고퀄 PNG 옷**으로 교체. (스펙 = 5번 섹션)

- 상의/하의 슬롯 분리 + 원피스 슬롯 추가 완료 (상·하의 5종 슬롯: 모자/상의/하의/원피스/신발).

**이후 후보**
- 다른 캐릭터 추가 (같은 방식: 전신 이미지 + quote + evalPrompt).
- 옷 카테고리 추가 확장 (액세서리·소품·망토 등).
- 가짜 평가 → 진짜 Claude LLM 평가 연결 (7번 참고).
