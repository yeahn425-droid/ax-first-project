# 옷입히기 게임 — AI 인수인계 핸드아웃

> 이 문서는 **다른 AI 툴에게 이 프로젝트를 이해시키기 위한** 자료입니다.
> 특히 지금 목표는 **게임에 넣을 "옷 PNG" 이미지를 다른 AI 그림 툴로 제작**하는 것이므로,
> 뒤쪽 [5. 옷 PNG 제작 스펙](#5-옷-png-제작-스펙-그림-ai-툴에게-줄-지시서) 부분이 가장 중요합니다.
> (그림 AI에게는 이 문서 전체가 아니라 5번 섹션만 복사해서 줘도 됩니다.)

---

## 1. 프로젝트 한눈에 보기

- **무엇인가**: 웹 브라우저에서 하는 **캐릭터 옷입히기 게임**.
- **누가 만드나**: 비개발자/기획자가 개발 학습 + 취미로 제작. **배포하지 않고 로컬(내 컴퓨터)에서만** 실행.
- **어떻게 실행하나**: 빌드 도구 없음. `index.html`을 브라우저로 열면 바로 실행됨.
- **기술 스택**: 순수 HTML + CSS + JavaScript (프레임워크 없음).

### 게임의 5단계 흐름
1. **게임 시작** — 캐릭터는 **피코 하나**. 시작 버튼을 누르면 **오늘의 컨셉(요구사항)이 랜덤으로 하나** 정해짐.
2. **옷 입히기** — 그 컨셉의 **대사(힌트)만 보고** 어울릴 것 같은 옷을 골라 입힘.
3. **숨은 요구조건** — 컨셉마다 "이런 옷을 원해요"라는 평가 기준이 있지만 **유저에게는 안 보임**. 유저는 대사로만 유추함. (이 기준은 나중에 LLM에게 넘길 프롬프트 문장으로 저장돼 있음.)
4. **런웨이** — 다 입히면 피코가 무대를 걸어감 (애니메이션 + 사운드).
5. **평가** — 입은 옷을 **그날 컨셉 기준으로** 채점해서 점수·코멘트를 보여줌. **지금은 가짜(규칙 기반) 평가**, 나중에 진짜 Claude LLM으로 교체 예정.

> 즉, 캐릭터는 피코 하나지만 **시작할 때마다 컨셉이 달라져서** 매번 다른 미션이 됩니다.

---

## 2. 파일 구조

```
ax-first-project/
├─ index.html      화면 뼈대 (3개 화면 + 팝업들)
├─ style.css       디자인/애니메이션
├─ game.js         게임 로직 (데이터 + 렌더링 + 사운드 + 평가) ← 핵심
├─ images/
│  ├─ Pico_muscle.png   피코 캐릭터 전신 이미지 (1114×1411)
│  └─ README.md
└─ HANDOFF.md      (이 문서)
```

3개 화면은 `index.html` 안에 다 들어있고, JS가 `hidden` 클래스를 껐다 켜서 전환합니다.
- `#screen-start` (게임 시작 — 피코 미리보기 + 시작 버튼)
- `#screen-dressup` (옷 입히기)
- `#screen-runway` (런웨이)

---

## 3. 화면 좌표계 (매우 중요)

캐릭터와 옷은 전부 **SVG `viewBox="0 0 300 400"`** 안에 그려집니다.
즉 **가로 300 × 세로 400짜리 좌표 도화지** 위에 모든 것이 얹힙니다.

- 캐릭터 전신 이미지(피코)는 이 도화지의 `x=2, y=8, width=296, height=384` 위치에 깔립니다.
- 옷은 그 **위에 겹쳐서** 그려집니다 (레이어 순서: 신발 → 상의 → 모자).

```
(0,0) ┌───────────────┐  ← viewBox 좌상단
      │   피코 이미지   │
      │   위에 옷을     │
      │   얹는 구조     │
      └───────────────┘ (300,400) ← viewBox 우하단
```

---

## 4. 데이터 모델 (game.js)

### 4-1. 캐릭터 + 컨셉
캐릭터는 피코 하나뿐이라 단순합니다.
```js
const PICO = { name: "피코", bodyImage: "images/Pico_muscle.png" };
```

요구사항은 **컨셉(`concepts` 배열)** 으로 분리했고, 시작할 때마다 랜덤으로 하나가 뽑힙니다.
```js
{
  id: "party",
  label: "우아한 파티룩",                              // (내부용 이름)
  quote: "오늘 저녁에 근사한 파티가 있어! 우아하게 부탁해 ✨",  // ★유저에게 보이는 유일한 힌트
  evalPrompt: "오늘 피코의 컨셉은 '우아한 파티룩'입니다...",     // ★유저에게 안 보임. 나중에 LLM 채점용
  likeTags: ["elegant", "cute"],    // (임시 가짜평가용) 이 컨셉이 좋아하는 옷 태그
  dislikeTags: ["casual", "sporty"],// (임시 가짜평가용) 싫어하는 옷 태그
}
```
- **`quote`(보임) vs `evalPrompt`(숨김)의 분리가 이 게임의 핵심 재미**입니다. 유저는 대사만 보고 숨은 취향을 추리합니다.
- 현재 컨셉 5종: 지적인 룩 / 우아한 파티룩 / 발랄 액티브룩 / 사랑스러운 데이트룩 / 개성 만점 룩. **컨셉을 늘리고 싶으면 `concepts` 배열에 같은 형식으로 추가**하면 됩니다.

### 4-2. 옷 (`wardrobe` 배열) — **지금 여기가 교체 대상**
현재 옷은 **손으로 그린 SVG 도형**으로 되어 있어 퀄리티가 낮습니다. 예:
```js
{
  id: "hat_straw", category: "hat", name: "밀짚모자",
  tags: ["cute", "casual"],           // (가짜평가용 성격 태그)
  onBody: `<ellipse .../><path .../>`, // ★캐릭터 위에 겹쳐 그릴 SVG (← 이걸 PNG로 바꿀 예정)
  icon:   `<ellipse .../>`,            // 옷장 버튼용 작은 아이콘 SVG
}
```
- 카테고리(`category`)는 현재 5종: **`hat`(모자) / `top`(상의) / `bottom`(하의) / `dress`(원피스) / `shoes`(신발)**. 각 카테고리당 하나씩만 착용 가능.
- **상·하의 vs 원피스 규칙**: 원피스는 상체~하체를 한 벌로 덮는 옷이라 **상의·하의와 동시에 입을 수 없습니다.** 코드에서 자동 처리됨 — 원피스를 입으면 상의·하의가 벗겨지고, 상의나 하의를 입으면 원피스가 벗겨집니다.
- **겹치는 순서(뒤→앞)**: 신발 → 하의 → 상의 → 원피스 → 모자.
- **옷 그림**: 상의·하의·원피스는 실제 이미지(자동 정렬)로 적용됨. 모자·신발은 아직 도형(SVG). (이미지 규격·정렬은 아래 5번.)

---

## 5. 옷 PNG 규격 + 자동 정렬 파이프라인 ★사이즈가 항상 맞는 핵심★

**옷 사이즈가 안 맞던 이유**: 옷 그림마다 캔버스 안 크기·위치가 제각각이라, 그대로 얹으면 어떤 건 크고 어떤 건 작았어요.

**해결(규격 통일)**: 옷을 **피코 몸 템플릿(1114×1411)의 '입었을 때 위치'에 자동으로 맞춰 배치**한 뒤, 게임에선 **모든 옷을 피코 몸과 똑같은 좌표로 1:1로 겹치기만** 합니다. → 옷마다 좌표를 재는 작업이 사라지고 사이즈가 항상 맞습니다.

### 5-1. 그림 만들 때 규격 (그림 AI 툴에게 줄 것)
- **배경**: 완전 투명(alpha) PNG. **한 파일 = 한 옷**.
- **내용**: "피코 근육질 몸에 그 옷을 입힌 모습"에서 **옷 부분만** (팔·다리까지 포함해서). 예) 상의면 근육 몸통+팔에 상의를 입힌 그림.
- **자세**: 정면, 똑바로 선 자세, 좌우 대칭, 캔버스 중앙 부근. (크기·여백은 신경 안 써도 됨 — 아래 정렬 툴이 맞춰줌)
- **캐릭터**: 피코 = 보라색 새 얼굴(안경) + 우스꽝스러운 근육 사람 몸. 참고 이미지 `images/Pico_muscle.png`.
- **아트 스타일**: 밝고 귀여운 카툰/플랫, 또렷한 외곽선, UI는 핑크/보라 파스텔.

### 5-2. 자동 정렬 툴 (`tools/align_clothes.js`)
raw 그림을 피코 몸에 맞춰주는 스크립트입니다. **사람이 좌표를 재지 않아도** 카테고리별 목표 영역에 자동 배치해요.
1. raw PNG를 `images/raw/`에 넣기.
2. `tools/align_clothes.js`의 `ITEMS`에 `{ raw:"파일명.png", id:"짧은id", category:"top|bottom|dress" }` 한 줄 추가.
3. 실행: `NODE_PATH=<repo>/node_modules node tools/align_clothes.js`
   (playwright-core 필요: `npm install playwright-core --no-save`)
4. 결과: `images/파일명.png`(전신 정렬본) + `images/icon_<id>.png`(옷장 썸네일)가 생성/갱신됨.

카테고리별 목표 영역(피코 캔버스 px)은 스크립트 `TARGETS`에 있어요: 상의/원피스는 어깨~허리(또는 허벅지)+팔, 하의는 허리~발목. 살짝 안 맞으면 이 숫자만 조정하고 다시 실행하면 됩니다.

### 5-3. 아직 이미지 없는 부위
- **모자(hat) / 신발(shoes)**: 아직 raw 이미지가 없어 도형(SVG)으로 표시 중. 이 둘은 **위치가 몸이 아니라 머리·발**이라, 이미지가 생기면 `TARGETS`에 `hat`(머리 위)·`shoes`(발) 목표를 추가해 정렬하면 됩니다.
- **치마(bottom)**: 도형 임시. `bottom`으로 raw 넣으면 청바지처럼 정렬됨.

---

## 6. 옷 이미지를 코드에 넣는 방법 (개발자용)

정렬된 이미지는 `game.js`의 헬퍼로 아주 짧게 연결합니다 (모두 같은 배치 규칙):
```js
{
  id: "top_sailor", category: "top", name: "세일러 상의", tags: ["cute","unique"],
  onBody: wornImg("Pico_sailor.png"),   // 피코 몸과 동일 좌표로 1:1 겹침
  icon:   iconImg("icon_sailor.png"),   // 미리 잘라둔 썸네일
}
```
- `wornImg(file)` = `<image ... x=2 y=8 width=296 height=384 preserveAspectRatio="xMidYMid meet">` (피코 `bodyImage`와 동일). 정렬 툴이 위치를 맞춰놨으니 좌표가 옷마다 다를 필요가 없음.
- 렌더링·착용·런웨이 로직은 `onBody` 문자열을 그대로 SVG에 삽입하므로 그 외 수정 불필요.

---

## 7. 평가 로직 (나중에 진짜 AI로 교체할 지점)

- 채점은 `game.js`의 **`evaluateOutfit(concept, worn)` 함수 한 곳**에서만 일어납니다.
- 지금은 인터넷/AI 없이 **규칙 기반 가짜 점수**(태그 매칭 +12 / 감점 -15 / 풀착용 +15, 기본 60점)를 냅니다.
- 나중에 이 함수 안에서 `concept.evalPrompt`(그날 컨셉의 숨은 기준)와 입은 옷 목록을 **Claude LLM에 보내 점수·코멘트를 받아오도록** 바꾸면 5단계가 "진짜 AI 평가"로 완성됩니다. **이 함수만 갈아끼우면 됨.**

---

## 8. 현재 상태 & 다음 할 일

**완료됨**
- 5단계 전체 뼈대 (선택→입히기→런웨이→평가) 동작.
- 게임 연출: 확인 팝업, 걷기 애니메이션, 두구두구 드럼롤, 효과음/BGM(사운드 파일 없이 Web Audio로 합성).
- 실제 캐릭터 **피코** 적용 + 옷 위치를 피코 몸 윤곽에 맞게 정밀 조정.

**지금 하려는 것 (이 문서의 목적)**
- 손그림 SVG 옷 → **그림 AI로 만든 고퀄 PNG 옷**으로 교체. (스펙 = 5번 섹션)

- 상의/하의 슬롯 분리 + 원피스 슬롯 추가 완료 (5종 슬롯: 모자/상의/하의/원피스/신발).
- 구조 변경: 캐릭터를 **피코 하나**로 고정, 캐릭터 선택창 → **게임 시작 화면**으로 교체. 요구사항을 **컨셉(랜덤)** 으로 분리 (컨셉 5종).
- **실제 옷 이미지 5종 적용 완료**: 상의(세일러/흰티/핑크후드), 하의(청바지), 원피스(프릴).
- **규격 통일 + 자동 정렬 파이프라인 도입** (`tools/align_clothes.js`): raw 그림을 피코 몸 위치에 자동 배치 → 게임은 모든 옷을 동일 좌표로 1:1 겹침 → **사이즈가 항상 맞음**. (원본은 `images/raw/`에 보관.)

**아직 이미지 없음 (도형 임시로 사용 중) — 만들면 교체 예정**
- 모자(hat), 신발(shoes), 체크 치마(bottom).

**이후 후보**
- 위 남은 부위 이미지 추가.
- 컨셉 더 추가 (`concepts` 배열에 quote + evalPrompt + 태그).
- 가짜 평가 → 진짜 Claude LLM 평가 연결.
- 옷 카테고리 추가 확장 (액세서리·소품·망토 등).
- 가짜 평가 → 진짜 Claude LLM 평가 연결 (7번 참고).
